// This file contains template functions to be inserted into dashboard.html
// Copy all ARP Both functions and replace "Both" with "Request" or "Reply"
// Replace base filter expressions:
// - Request: 'arp[6:2] == 1 and'
// - Reply: 'arp[6:2] == 2 and'
